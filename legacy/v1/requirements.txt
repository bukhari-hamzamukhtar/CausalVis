# Core dependencies (pinned for reproducible environment)
pycocotools==2.0.11
networkx==3.2.1
matplotlib==3.9.4

# PyTorch (CPU wheel) - install from the PyTorch index when creating the venv:
# Example: python -m pip install --index-url https://download.pytorch.org/whl/cpu "torch==2.8.0+cpu"
torch==2.8.0+cpu

# PyTorch Geometric
torch_geometric==2.6.1

# Optional metric helpers
torchmetrics==1.9.0